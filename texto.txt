webapp/
   index.html
   styles.css
   app.js const API_URL = "https://jfgeneral-backend2-production.up.railway.app";
   assets/
       logo.png
       favicon.png
       otras imágenes (si tienes)
async function testBackend() {
  try {
    const res = await fetch(API_URL + "/");
    const data = await res.json();
    alert("Servidor dice: " + data.message);
  } catch (err) {
    console.error(err);
    alert("Error conectando con el servidor");
  }
}

document.addEventListener("DOMContentLoaded", () => {
  const btn = document.getElementById("test-backend-btn");
  if (btn) {
    btn.addEventListener("click", testBackend);
  }
});
